const initialState = {
    isAuthenticated: false,
    user:{
        },
    schemes: [],
    
  }

  export default (state, action) =>{
    switch(action.type){
        default: return state
    }
  }